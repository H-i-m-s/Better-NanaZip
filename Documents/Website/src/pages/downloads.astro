---
import Layout from "../layouts/Layout.astro";
import InfoBar from "../components/InfoBar/InfoBar.astro";
---

<Layout title="下载" description="NanaZip 定制版下载">
    <main id="main">
        <article>
            <h1>下载</h1>

            <InfoBar title="注意" severity="attention" wrap={true}>
                本项目仅通过
                <a href="https://github.com/H-i-m-s/Better-NanaZip">GitHub</a>
                官方 Releases 发布，请勿从其他来源下载，谨防假冒与恶意修改。
            </InfoBar>

            <!-- GitHub Releases -->
            <h2>GitHub Releases</h2>

            <p>下载最新稳定版安装包（MSIX）：</p>
            <a class="btn btn-accent" href="https://github.com/H-i-m-s/Better-NanaZip/releases/latest">
                最新稳定版
            </a>
            <br />
            <a href="https://github.com/H-i-m-s/Better-NanaZip/releases">所有版本</a>

            <h2>安装说明</h2>

            <p>直接安装：</p>

            <pre><code>Add-AppxPackage -Path "NanaZipPackage_x.x.x.x_x64_arm64.msixbundle"</code></pre>

            <InfoBar title="提示" severity="information" wrap={true}>
                安装后如右键菜单未出现，请在任务管理器中重启资源管理器
                （explorer.exe）。
            </InfoBar>

            <h2>发布内容</h2>

            <ul>
                <li><b>MSIX 安装包</b>（x64 + ARM64，推荐）</li>
                <li><b>扩展包安装器</b>（右键菜单增强）</li>
                <li><b>便携版压缩包</b>（无需安装，适合特殊环境）</li>
                <li><b>调试符号</b>（供开发者排查问题）</li>
            </ul>
        </article>
    </main>
</Layout>

<style>
    a {
        margin-top: 8px;
        margin-bottom: 8px;
    }
    pre {
        background: var(--fds-card-background-default);
        border: 1px solid var(--fds-card-stroke-default);
        border-radius: 8px;
        padding: 12px 16px;
        overflow-x: auto;
    }
    code {
        font-family: Consolas, Menlo, monospace;
        font-size: 13px;
    }
</style>
