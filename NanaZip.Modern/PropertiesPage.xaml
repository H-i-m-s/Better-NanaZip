<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NanaZip.Modern.PropertiesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    IsTextScaleFactorEnabled="False"
    KeyDown="OnPageKeyDown"
    PreviewKeyDown="OnPageKeyDown"
    Background="Transparent">

    <Page.Resources>
        <!-- Details tab: property name / value two-column list -->
        <DataTemplate x:Key="DetailItemTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    Text="{Binding Name}"
                    TextWrapping="Wrap"
                    FontWeight="SemiBold" />
                <TextBlock
                    Grid.Column="1"
                    Text="{Binding Value}"
                    TextWrapping="Wrap"
                    Margin="14,0,0,0" />
            </Grid>
        </DataTemplate>

        <!-- Signature tab: signer + status + detail -->
        <DataTemplate x:Key="SignatureItemTemplate">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock
                        Text="{Binding Signer}"
                        FontWeight="SemiBold"
                        TextTrimming="CharacterEllipsis" />
                    <TextBlock
                        Text="{Binding Status}"
                        Margin="10,0,0,0"
                        Opacity="0.75"
                        TextTrimming="CharacterEllipsis" />
                </StackPanel>
                <TextBlock
                    Text="{Binding Detail}"
                    TextWrapping="Wrap"
                    FontSize="12"
                    Opacity="0.6"
                    Margin="0,2,0,0" />
            </StackPanel>
        </DataTemplate>

        <!-- Security tab: principal + permission summary -->
        <DataTemplate x:Key="SecurityItemTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="220" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    Text="{Binding Name}"
                    TextWrapping="Wrap"
                    FontWeight="SemiBold" />
                <TextBlock
                    Grid.Column="1"
                    Text="{Binding Access}"
                    TextWrapping="Wrap"
                    Margin="14,0,0,0" />
            </Grid>
        </DataTemplate>

        <!-- Previous versions tab: time + location -->
        <DataTemplate x:Key="VersionItemTemplate">
            <StackPanel>
                <TextBlock
                    Text="{Binding Time}"
                    FontWeight="SemiBold"
                    TextTrimming="CharacterEllipsis" />
                <TextBlock
                    Text="{Binding Location}"
                    TextWrapping="Wrap"
                    FontSize="12"
                    Opacity="0.6"
                    Margin="0,2,0,0" />
            </StackPanel>
        </DataTemplate>

        <!-- Custom tab: property name + type + value -->
        <DataTemplate x:Key="CustomPropertyItemTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    Text="{Binding Name}"
                    TextWrapping="Wrap"
                    FontWeight="SemiBold"
                    Margin="6,4,6,4" />
                <TextBlock
                    Grid.Column="1"
                    Text="{Binding Type}"
                    Opacity="0.7"
                    Margin="14,4,6,4" />
                <TextBlock
                    Grid.Column="2"
                    Text="{Binding Value}"
                    TextWrapping="Wrap"
                    Margin="14,4,6,4" />
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <!-- Header, tab bar, content, button row. The button row stays
                 reachable when the window is short. -->
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- ============ Header: large icon + name + type ============ -->
        <Grid Grid.Row="0" Margin="12,12,12,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Image
                x:Name="HeaderIconImage"
                Width="48"
                Height="48"
                Margin="0,0,14,0"
                VerticalAlignment="Top" />
            <StackPanel Grid.Column="1" VerticalAlignment="Top">
                <TextBlock
                    x:Name="HeaderNameText"
                    FontSize="18"
                    FontWeight="SemiBold"
                    TextWrapping="Wrap"
                    TextTrimming="CharacterEllipsis" />
                <TextBlock
                    x:Name="HeaderTypeText"
                    FontSize="13"
                    Opacity="0.7"
                    Margin="0,2,0,0"
                    TextWrapping="Wrap"
                    TextTrimming="CharacterEllipsis" />
            </StackPanel>
        </Grid>

        <!-- ============ Tab bar (mimics the property sheet tabs) ============ -->
        <StackPanel
            x:Name="TabBar"
            Grid.Row="1"
            Orientation="Horizontal"
            Padding="12,8,12,0"
            Margin="0,0,0,6">
            <ToggleButton
                x:Name="TabGeneralButton"
                Padding="12,4"
                Margin="0,0,4,0"
                Click="TabButtonClick" />
            <ToggleButton
                x:Name="TabDetailsButton"
                Padding="12,4"
                Margin="0,0,4,0"
                Click="TabButtonClick" />
            <ToggleButton
                x:Name="TabSignatureButton"
                Padding="12,4"
                Margin="0,0,4,0"
                Click="TabButtonClick" />
            <ToggleButton
                x:Name="TabSecurityButton"
                Padding="12,4"
                Margin="0,0,4,0"
                Click="TabButtonClick" />
            <ToggleButton
                x:Name="TabVersionsButton"
                Padding="12,4"
                Margin="0,0,4,0"
                Click="TabButtonClick" />
            <ToggleButton
                x:Name="TabCustomButton"
                Padding="12,4"
                Margin="0,0,4,0"
                Click="TabButtonClick" />
        </StackPanel>

        <!-- ============ Content area ============ -->
        <Grid Grid.Row="2" Margin="12,0,12,0">
            <Border
                BorderBrush="{ThemeResource CardStrokeColorDefault}"
                BorderThickness="0,1,0,0" />

            <!-- General tab -->
            <ScrollViewer
                x:Name="GeneralTabPanel"
                ScrollViewer.VerticalScrollBarVisibility="Auto">
                <StackPanel Padding="2,10,2,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                        <TextBlock
                            x:Name="GeneralTypeLabel"
                            Text="类型"
                            Opacity="0.75"
                            VerticalAlignment="Top" />
                        <TextBlock
                            x:Name="GeneralTypeValueText"
                            Grid.Column="1"
                            TextWrapping="Wrap"
                            Margin="14,0,0,6" />
                        <TextBlock
                            x:Name="GeneralLocationLabel"
                            Grid.Row="1"
                            Text="位置"
                            Opacity="0.75"
                            VerticalAlignment="Top" />
                        <TextBlock
                            x:Name="GeneralLocationValueText"
                            Grid.Row="1"
                            Grid.Column="1"
                            TextWrapping="Wrap"
                            Margin="14,0,0,6" />
                        <TextBlock
                            x:Name="GeneralSizeLabel"
                            Grid.Row="2"
                            Text="大小"
                            Opacity="0.75"
                            VerticalAlignment="Top" />
                        <TextBlock
                            x:Name="GeneralSizeValueText"
                            Grid.Row="2"
                            Grid.Column="1"
                            TextWrapping="Wrap"
                            Margin="14,0,0,6" />
                        <TextBlock
                            x:Name="GeneralAllocSizeLabel"
                            Grid.Row="3"
                            Text="占用空间"
                            Opacity="0.75"
                            VerticalAlignment="Top" />
                        <TextBlock
                            x:Name="GeneralAllocSizeValueText"
                            Grid.Row="3"
                            Grid.Column="1"
                            TextWrapping="Wrap"
                            Margin="14,0,0,6" />
                        <TextBlock
                            x:Name="GeneralCreatedLabel"
                            Grid.Row="4"
                            Text="创建时间"
                            Opacity="0.75"
                            VerticalAlignment="Top" />
                        <TextBlock
                            x:Name="GeneralCreatedValueText"
                            Grid.Row="4"
                            Grid.Column="1"
                            TextWrapping="Wrap"
                            Margin="14,0,0,6" />
                        <TextBlock
                            x:Name="GeneralModifiedLabel"
                            Grid.Row="5"
                            Text="修改时间"
                            Opacity="0.75"
                            VerticalAlignment="Top" />
                        <TextBlock
                            x:Name="GeneralModifiedValueText"
                            Grid.Row="5"
                            Grid.Column="1"
                            TextWrapping="Wrap"
                            Margin="14,0,0,6" />
                        <TextBlock
                            x:Name="GeneralAccessedLabel"
                            Grid.Row="6"
                            Text="访问时间"
                            Opacity="0.75"
                            VerticalAlignment="Top" />
                        <TextBlock
                            x:Name="GeneralAccessedValueText"
                            Grid.Row="6"
                            Grid.Column="1"
                            TextWrapping="Wrap"
                            Margin="14,0,0,6" />
                    </Grid>

                    <Border
                        Height="1"
                        Background="{ThemeResource CardStrokeColorDefault}"
                        Margin="0,12,0,10" />

                    <TextBlock
                        Text="属性"
                        FontWeight="SemiBold"
                        Margin="0,0,0,8" />
                    <CheckBox
                        x:Name="GeneralReadOnlyCheck"
                        Content="只读"
                        Margin="0,0,0,6"
                        Click="OnReadOnlyCheckClicked" />
                    <CheckBox
                        x:Name="GeneralHiddenCheck"
                        Content="隐藏"
                        Click="OnHiddenCheckClicked" />
                </StackPanel>
            </ScrollViewer>

            <!-- Details tab -->
            <Grid
                x:Name="DetailsTabPanel"
                Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBox
                    x:Name="DetailsSearchBox"
                    Margin="0,10,0,8"
                    PlaceholderText="搜索属性"
                    TextChanged="OnDetailsSearchChanged" />
                <ListView
                    x:Name="DetailsListView"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Grid.Row="1"
                    ItemTemplate="{StaticResource DetailItemTemplate}"
                    SelectionMode="None">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="MinHeight" Value="0" />
                            <Setter Property="Padding" Value="6,4,6,4" />
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
            </Grid>

            <!-- Digital signature tab -->
            <Grid
                x:Name="SignatureTabPanel"
                Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock
                    x:Name="SignatureStatusText"
                    Margin="0,10,0,8"
                    TextWrapping="Wrap" />
                <ListView
                    x:Name="SignatureListView"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Grid.Row="1"
                    ItemTemplate="{StaticResource SignatureItemTemplate}"
                    SelectionMode="None">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="MinHeight" Value="0" />
                            <Setter Property="Padding" Value="6,4,6,4" />
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
            </Grid>

            <!-- Security tab -->
            <Grid
                x:Name="SecurityTabPanel"
                Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock
                    x:Name="SecurityOwnerText"
                    Margin="0,10,0,8"
                    TextWrapping="Wrap" />
                <Grid
                    Grid.Row="1"
                    Margin="0,0,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="220" />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        Text="用户或组"
                        Opacity="0.75" />
                    <TextBlock
                        Grid.Column="1"
                        Text="权限"
                        Margin="14,0,0,0"
                        Opacity="0.75" />
                </Grid>
                <ListView
                    x:Name="SecurityListView"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Grid.Row="2"
                    ItemTemplate="{StaticResource SecurityItemTemplate}"
                    SelectionMode="None">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="MinHeight" Value="0" />
                            <Setter Property="Padding" Value="6,4,6,4" />
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
                <StackPanel
                    Grid.Row="3"
                    Orientation="Horizontal"
                    Margin="0,8,0,0">
                    <Button
                        x:Name="SecurityAdvancedButton"
                        MinWidth="110"
                        Content="高级设置"
                        Click="OnSecurityAdvancedClicked" />
                </StackPanel>
            </Grid>

            <!-- Previous versions tab -->
            <Grid
                x:Name="VersionsTabPanel"
                Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock
                    x:Name="VersionsStatusText"
                    Margin="0,10,0,8"
                    TextWrapping="Wrap" />
                <ListView
                    x:Name="VersionsListView"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Grid.Row="1"
                    ItemTemplate="{StaticResource VersionItemTemplate}"
                    SelectionChanged="OnVersionSelectionChanged">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="MinHeight" Value="0" />
                            <Setter Property="Padding" Value="6,4,6,4" />
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
                <StackPanel
                    Grid.Row="2"
                    Orientation="Horizontal"
                    Margin="0,8,0,0">
                    <Button
                        x:Name="OpenVersionButton"
                        MinWidth="88"
                        Content="打开"
                        IsEnabled="False"
                        Click="OnOpenVersionClicked" />
                    <Button
                        x:Name="CopyVersionButton"
                        MinWidth="88"
                        Margin="6,0,0,0"
                        Content="复制"
                        IsEnabled="False"
                        Click="OnCopyVersionClicked" />
                </StackPanel>
            </Grid>

            <!-- Custom tab -->
            <Grid
                x:Name="CustomTabPanel"
                Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock
                    x:Name="CustomStatusText"
                    Margin="0,10,0,8"
                    TextWrapping="Wrap" />
                <ListView
                    x:Name="CustomListView"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    ScrollViewer.HorizontalScrollMode="Disabled"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                    Grid.Row="1"
                    ItemTemplate="{StaticResource CustomPropertyItemTemplate}"
                    SelectionChanged="OnCustomSelectionChanged">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsStackPanel HorizontalAlignment="Stretch" />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalAlignment" Value="Stretch" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="MinWidth" Value="0" />
                            <Setter Property="MinHeight" Value="0" />
                            <Setter Property="Padding" Value="0" />
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
                <StackPanel
                    Grid.Row="2"
                    Margin="0,8,0,0">
                    <StackPanel Orientation="Horizontal">
                        <Button
                            x:Name="AddCustomButton"
                            MinWidth="88"
                            Content="添加"
                            Click="OnAddCustomClicked" />
                        <Button
                            x:Name="EditCustomButton"
                            MinWidth="88"
                            Margin="6,0,0,0"
                            Content="编辑"
                            IsEnabled="False"
                            Click="OnEditCustomClicked" />
                        <Button
                            x:Name="DeleteCustomButton"
                            MinWidth="88"
                            Margin="6,0,0,0"
                            Content="删除"
                            IsEnabled="False"
                            Click="OnDeleteCustomClicked" />
                    </StackPanel>
                    <StackPanel
                        x:Name="CustomEditPanel"
                        Visibility="Collapsed"
                        Margin="0,10,0,0">
                        <ComboBox
                            x:Name="CustomNameCombo"
                            Margin="0,0,0,6"
                            HorizontalAlignment="Stretch" />
                        <ComboBox
                            x:Name="CustomTypeCombo"
                            Margin="0,0,0,6"
                            HorizontalAlignment="Stretch" />
                        <TextBox
                            x:Name="CustomValueBox"
                            Margin="0,0,0,8"
                            HorizontalAlignment="Stretch" />
                        <StackPanel
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">
                            <Button
                                x:Name="CustomEditOkButton"
                                MinWidth="88"
                                Content="确定"
                                Click="OnCustomEditOkClicked" />
                            <Button
                                x:Name="CustomEditCancelButton"
                                MinWidth="88"
                                Margin="6,0,0,0"
                                Content="取消"
                                Click="OnCustomEditCancelClicked" />
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Grid>

        <!-- ============ Button row ============ -->
        <StackPanel
            Grid.Row="3"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            Margin="12,10,12,12">
            <Button
                x:Name="OkButton"
                x:Uid="/NanaZip.Modern/Common/OKButton"
                MinWidth="88"
                Click="OnOkClicked" />
            <Button
                x:Name="CancelButton"
                x:Uid="/NanaZip.Modern/Common/CancelButton"
                MinWidth="88"
                Margin="6,0,0,0"
                Click="OnCancelClicked" />
            <Button
                x:Name="ApplyButton"
                MinWidth="88"
                Margin="6,0,0,0"
                Content="应用"
                IsEnabled="False"
                Click="OnApplyClicked" />
        </StackPanel>
    </Grid>
</Page>
