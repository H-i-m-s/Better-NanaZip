<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NanaZip.Modern.ExtractPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    KeyDown="OnPageKeyDown"
    PreviewKeyDown="OnPageKeyDown"
    Background="Transparent">

    <Page.Resources>
        <DataTemplate x:Key="WrapCheckTextTemplate">
            <TextBlock Text="{Binding}" TextWrapping="Wrap"/>
        </DataTemplate>
        <DataTemplate x:Key="WrapComboItemTemplate">
            <TextBlock
                Text="{Binding}"
                TextWrapping="Wrap"
                MaxWidth="320"/>
        </DataTemplate>
        <Style x:Key="PathTextBoxStyle" TargetType="TextBox">
            <Setter Property="Foreground" Value="{ThemeResource TextControlForeground}" />
            <Setter Property="Background" Value="{ThemeResource TextControlBackground}" />
            <Setter Property="BorderBrush" Value="{ThemeResource ControlStrokeColorDefaultBrush}" />
            <Setter Property="BorderThickness" Value="{ThemeResource TextControlBorderThemeThickness}" />
            <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}" />
            <Setter Property="FontSize" Value="{ThemeResource ControlContentThemeFontSize}" />
            <Setter Property="ScrollViewer.HorizontalScrollMode" Value="Auto" />
            <Setter Property="ScrollViewer.VerticalScrollMode" Value="Disabled" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Hidden" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Hidden" />
            <Setter Property="MinHeight" Value="{ThemeResource TextControlThemeMinHeight}" />
            <Setter Property="Padding" Value="10,5,32,6" />
            <Setter Property="CornerRadius" Value="{ThemeResource ControlCornerRadius}" />
            <Setter Property="BackgroundSizing" Value="InnerBorderEdge" />
            <Setter Property="ContextFlyout" Value="{ThemeResource TextControlCommandBarContextFlyout}" />
            <Setter Property="SelectionHighlightColor" Value="{ThemeResource TextControlSelectionHighlightColor}" />
            <Setter Property="UseSystemFocusVisuals" Value="{ThemeResource IsApplicationFocusVisualKindReveal}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <ContentPresenter
                                x:Name="HeaderContentPresenter"
                                Grid.Row="0"
                                Margin="{ThemeResource TextBoxTopHeaderMargin}"
                                VerticalAlignment="Top"
                                x:DeferLoadStrategy="Lazy"
                                Content="{TemplateBinding Header}"
                                ContentTemplate="{TemplateBinding HeaderTemplate}"
                                FontWeight="Normal"
                                Foreground="{ThemeResource TextControlHeaderForeground}"
                                TextWrapping="Wrap"
                                Visibility="Collapsed" />
                            <Border
                                x:Name="BorderElement"
                                Grid.Row="1"
                                MinWidth="{TemplateBinding MinWidth}"
                                MinHeight="{TemplateBinding MinHeight}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Control.IsTemplateFocusTarget="True"
                                CornerRadius="{TemplateBinding CornerRadius}" />
                            <ScrollViewer
                                x:Name="ContentElement"
                                Grid.Row="1"
                                Margin="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                AutomationProperties.AccessibilityView="Raw"
                                Foreground="{TemplateBinding Foreground}"
                                HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                HorizontalScrollMode="{TemplateBinding ScrollViewer.HorizontalScrollMode}"
                                IsTabStop="False"
                                VerticalScrollBarVisibility="Hidden"
                                VerticalScrollMode="Disabled"
                                ZoomMode="Disabled" />
                            <TextBlock
                                x:Name="PlaceholderTextContentPresenter"
                                Grid.Row="1"
                                Margin="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                Foreground="{Binding PlaceholderForeground, RelativeSource={RelativeSource TemplatedParent}, TargetNullValue={ThemeResource TextControlPlaceholderForeground}}"
                                IsHitTestVisible="False"
                                Text="{TemplateBinding PlaceholderText}"
                                TextAlignment="{TemplateBinding TextAlignment}"
                                TextWrapping="{TemplateBinding TextWrapping}" />
                            <ContentPresenter
                                x:Name="DescriptionPresenter"
                                Grid.Row="2"
                                x:Load="False"
                                AutomationProperties.AccessibilityView="Raw"
                                Content="{TemplateBinding Description}"
                                Foreground="{ThemeResource SystemControlDescriptionTextForegroundBrush}" />
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="Disabled">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="HeaderContentPresenter" Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlHeaderForegroundDisabled}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlBackgroundDisabled}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="BorderBrush">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlBorderBrushDisabled}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentElement" Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlForegroundDisabled}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="PlaceholderTextContentPresenter" Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{Binding PlaceholderForeground, RelativeSource={RelativeSource TemplatedParent}, TargetNullValue={ThemeResource TextControlPlaceholderForegroundDisabled}}" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="BorderBrush">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource ControlStrokeColorDefaultBrush}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlBackgroundPointerOver}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="PlaceholderTextContentPresenter" Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{Binding PlaceholderForeground, RelativeSource={RelativeSource TemplatedParent}, TargetNullValue={ThemeResource TextControlPlaceholderForegroundPointerOver}}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentElement" Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlForegroundPointerOver}" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Focused">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="PlaceholderTextContentPresenter" Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{Binding PlaceholderForeground, RelativeSource={RelativeSource TemplatedParent}, TargetNullValue={ThemeResource TextControlPlaceholderForegroundFocused}}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlBackgroundFocused}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="BorderBrush">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlBorderBrushFocused}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="BorderThickness">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlBorderThemeThicknessFocused}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentElement" Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlForegroundFocused}" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="HistoryArrowButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="MinWidth" Value="0" />
            <Setter Property="MinHeight" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid Background="{TemplateBinding Background}">
                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Padding="12,12,12,12">
        <Grid.RowDefinitions>
            <!-- The content row shrinks when the user compresses the window;
                 the button row stays at its natural height so OK/Cancel are
                 never clipped. -->
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left column -->
            <StackPanel x:Name="LeftColumnPanel" Grid.Column="0" Margin="0,0,12,0">
                <TextBlock
                    x:Name="ExtractToText"
                    TextWrapping="Wrap"/>
                <Grid Margin="0,6,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Column="0">
                        <TextBox
                            x:Name="PathBox"
                            Style="{StaticResource PathTextBoxStyle}"
                            MinWidth="120"
                            HorizontalAlignment="Stretch">
                            <FlyoutBase.AttachedFlyout>
                                <Flyout
                                    x:Name="HistoryFlyout"
                                    Placement="Bottom"
                                    Opening="OnHistoryFlyoutOpening"
                                    Opened="OnHistoryFlyoutOpened"
                                    Closed="OnHistoryFlyoutClosed">
                                    <Flyout.FlyoutPresenterStyle>
                                        <Style TargetType="FlyoutPresenter">
                                            <Setter Property="Padding" Value="0" />
                                            <Setter Property="MinWidth" Value="0" />
                                            <Setter Property="MaxWidth" Value="800" />
                                        </Style>
                                    </Flyout.FlyoutPresenterStyle>
                                    <ListView
                                        x:Name="HistoryList"
                                        IsItemClickEnabled="True"
                                        SelectionMode="None"
                                        MaxHeight="240"
                                        ItemClick="OnHistoryItemClicked">
                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem">
                                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                <Setter Property="MinHeight" Value="0" />
                                                <Setter Property="Padding" Value="10,6,6,6" />
                                            </Style>
                                        </ListView.ItemContainerStyle>
                                        <ListView.ItemTemplate>
                                            <DataTemplate>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock
                                                        Grid.Column="0"
                                                        Text="{Binding}"
                                                        TextWrapping="NoWrap"
                                                        TextTrimming="CharacterEllipsis"
                                                        VerticalAlignment="Center"
                                                        Margin="0,0,4,0" />
                                                    <Button
                                                        Grid.Column="1"
                                                        Content="&#x2715;"
                                                        Click="OnDeleteHistoryPathClicked"
                                                        ClickMode="Press"
                                                        Background="Transparent"
                                                        BorderThickness="0"
                                                        Padding="4"
                                                        MinWidth="0"
                                                        MinHeight="0"
                                                        VerticalAlignment="Center" />
                                                </Grid>
                                            </DataTemplate>
                                        </ListView.ItemTemplate>
                                    </ListView>
                                </Flyout>
                            </FlyoutBase.AttachedFlyout>
                        </TextBox>
                        <Button
                            x:Name="HistoryButton"
                            Style="{StaticResource HistoryArrowButtonStyle}"
                            HorizontalAlignment="Right"
                            Width="32"
                            VerticalAlignment="Stretch"
                            IsTabStop="False"
                            Click="OnHistoryButtonClicked">
                            <FontIcon
                                FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                FontSize="10"
                                Glyph="&#xE70D;"
                                Foreground="{ThemeResource TextControlForeground}" />
                        </Button>
                    </Grid>
                    <Button
                        x:Name="BrowseButton"
                        Grid.Column="1"
                        Margin="6,0,0,0"
                        Content="..."
                        Click="OnBrowseClicked"/>
                </Grid>

                <Grid Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <CheckBox
                        x:Name="NameEnableCheck"
                        Grid.Column="0"
                        MinWidth="0"
                        VerticalAlignment="Center"
                        Click="OnNameEnableClicked"/>
                    <TextBox
                        x:Name="NameBox"
                        Grid.Column="1"
                        MinWidth="120"
                        Margin="4,0,0,0"
                        VerticalAlignment="Center"/>
                </Grid>

                <!-- Path mode: the label keeps one line; when the window is
                     squeezed too narrow, the combo moves to the next row
                     (handled in UpdateModeRowLayout). -->
                <Grid x:Name="PathModeRow" Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        x:Name="PathModeText"
                        Grid.Row="0"
                        Grid.Column="0"
                        Margin="28,0,0,0"
                        VerticalAlignment="Center"/>
                    <ComboBox
                        x:Name="PathModeCombo"
                        Grid.Row="0"
                        Grid.Column="1"
                        Margin="6,0,0,0"
                        MinWidth="140"
                        HorizontalAlignment="Stretch"
                        ItemTemplate="{StaticResource WrapComboItemTemplate}"/>
                </Grid>

                <CheckBox
                    x:Name="ElimDupCheck"
                    Margin="0,10,0,0"
                    ContentTemplate="{StaticResource WrapCheckTextTemplate}"
                    Click="OnElimDupClicked"/>

                <!-- Overwrite mode: same label/combo wrap behavior. -->
                <Grid x:Name="OverwriteModeRow" Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        x:Name="OverwriteModeText"
                        Grid.Row="0"
                        Grid.Column="0"
                        Margin="28,0,0,0"
                        VerticalAlignment="Center"/>
                    <ComboBox
                        x:Name="OverwriteModeCombo"
                        Grid.Row="0"
                        Grid.Column="1"
                        Margin="6,0,0,0"
                        MinWidth="160"
                        HorizontalAlignment="Stretch"
                        ItemTemplate="{StaticResource WrapComboItemTemplate}"/>
                </Grid>
            </StackPanel>

            <!-- Right column -->
            <StackPanel x:Name="RightColumnPanel" Grid.Column="1" Margin="12,0,0,0">
                <Border
                    BorderBrush="#80808080"
                    BorderThickness="1"
                    CornerRadius="2"
                    Padding="8">
                    <StackPanel>
                        <TextBlock
                            x:Name="PasswordGroupText"/>
                        <Grid Margin="0,6,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <PasswordBox
                                x:Name="PasswordBox"
                                Grid.Column="0"
                                Background="{ThemeResource TextControlBackground}"
                                Foreground="{ThemeResource TextControlForeground}"
                                BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                BorderThickness="{ThemeResource TextControlBorderThemeThickness}"
                                PasswordChanged="OnPasswordChanged"/>
                            <Button
                                x:Name="AddPasswordButton"
                                Grid.Column="1"
                                Margin="6,0,0,0"
                                VerticalAlignment="Stretch"
                                Click="OnAddPasswordClicked">
                                <FontIcon
                                    x:Name="AddPasswordGlyph"
                                    Glyph="&#xE710;"
                                    FontSize="14"/>
                            </Button>
                        </Grid>
                        <!-- Cloud / local password lookup buttons: UI only
                             for now, the handlers are placeholders. -->
                        <StackPanel x:Name="PasswordButtonsPanel" Orientation="Horizontal" Margin="0,8,0,0">
                            <Button
                                x:Name="CloudPasswordButton"
                                Click="OnCloudPasswordClicked"/>
                            <Button
                                x:Name="LocalPasswordButton"
                                Margin="6,0,0,0"
                                Click="OnLocalPasswordClicked"/>
                        </StackPanel>
                        <!-- Inline local-match result (no nested modal
                             window; theme font size and colors are
                             inherited from the dialog). -->
                        <TextBlock
                            x:Name="MatchStatusText"
                            Margin="0,6,0,0"
                            TextWrapping="Wrap"
                            Visibility="Collapsed"/>
                        <CheckBox
                            x:Name="SharePasswordCheck"
                            Margin="0,8,0,0"
                            ContentTemplate="{StaticResource WrapCheckTextTemplate}"
                            Click="OnSharePasswordClicked"/>
                        <CheckBox
                            x:Name="ShowPasswordCheck"
                            Margin="0,8,0,0"
                            ContentTemplate="{StaticResource WrapCheckTextTemplate}"
                            Click="OnShowPasswordClicked"/>
                    </StackPanel>
                </Border>

                <CheckBox
                    x:Name="NtSecurityCheck"
                    Margin="8,10,0,0"
                    ContentTemplate="{StaticResource WrapCheckTextTemplate}"
                    Click="OnNtSecurityClicked"/>

                <CheckBox
                    x:Name="OpenFolderCheck"
                    Margin="8,6,0,0"
                    ContentTemplate="{StaticResource WrapCheckTextTemplate}"
                    Click="OnOpenFolderClicked"/>

                <CheckBox
                    x:Name="DeleteAfterCheck"
                    Margin="8,6,0,0"
                    ContentTemplate="{StaticResource WrapCheckTextTemplate}"
                    Click="OnDeleteAfterClicked"/>
            </StackPanel>
        </Grid>

        <StackPanel
            Grid.Row="1"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Margin="0,12,0,0">
            <Button
                x:Name="OkButton"
                MinWidth="88"
                Margin="0,0,6,0"
                Click="OnOkClicked"/>
            <Button
                x:Name="CancelButton"
                MinWidth="88"
                Click="OnCancelClicked"/>
        </StackPanel>
    </Grid>
</Page>
