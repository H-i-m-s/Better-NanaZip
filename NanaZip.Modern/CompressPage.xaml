<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NanaZip.Modern.CompressPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    KeyDown="OnPageKeyDown"
    PreviewKeyDown="OnPageKeyDown"
    Background="Transparent">

    <Page.Resources>
        <DataTemplate x:Key="WrapItemTemplate">
            <TextBlock
                Text="{Binding}"
                TextWrapping="NoWrap"
                TextTrimming="CharacterEllipsis"
                MaxWidth="360" />
        </DataTemplate>
        <!-- TextBox template without the built-in clear (X) button.
             Copied from AddressBarTemplate and reduced to a single column. -->
        <Style x:Key="PathTextBoxStyle" TargetType="TextBox">
            <Setter Property="Foreground" Value="{ThemeResource TextControlForeground}" />
            <Setter Property="Background" Value="{ThemeResource TextControlBackground}" />
            <Setter Property="BorderBrush" Value="{ThemeResource ControlStrokeColorDefaultBrush}" />
            <Setter Property="BorderThickness" Value="{ThemeResource TextControlBorderThemeThickness}" />
            <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}" />
            <Setter Property="FontSize" Value="{ThemeResource ControlContentThemeFontSize}" />
            <Setter Property="ScrollViewer.HorizontalScrollMode" Value="Auto" />
            <Setter Property="ScrollViewer.VerticalScrollMode" Value="Disabled" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Hidden" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Hidden" />
            <Setter Property="MinHeight" Value="{ThemeResource TextControlThemeMinHeight}" />
            <Setter Property="Padding" Value="10,5,32,6" />
            <Setter Property="CornerRadius" Value="{ThemeResource ControlCornerRadius}" />
            <Setter Property="BackgroundSizing" Value="InnerBorderEdge" />
            <Setter Property="ContextFlyout" Value="{ThemeResource TextControlCommandBarContextFlyout}" />
            <Setter Property="SelectionHighlightColor" Value="{ThemeResource TextControlSelectionHighlightColor}" />
            <Setter Property="UseSystemFocusVisuals" Value="{ThemeResource IsApplicationFocusVisualKindReveal}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <ContentPresenter
                                x:Name="HeaderContentPresenter"
                                Grid.Row="0"
                                Margin="{ThemeResource TextBoxTopHeaderMargin}"
                                VerticalAlignment="Top"
                                x:DeferLoadStrategy="Lazy"
                                Content="{TemplateBinding Header}"
                                ContentTemplate="{TemplateBinding HeaderTemplate}"
                                FontWeight="Normal"
                                Foreground="{ThemeResource TextControlHeaderForeground}"
                                TextWrapping="Wrap"
                                Visibility="Collapsed" />
                            <Border
                                x:Name="BorderElement"
                                Grid.Row="1"
                                MinWidth="{TemplateBinding MinWidth}"
                                MinHeight="{TemplateBinding MinHeight}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Control.IsTemplateFocusTarget="True"
                                CornerRadius="{TemplateBinding CornerRadius}" />
                            <ScrollViewer
                                x:Name="ContentElement"
                                Grid.Row="1"
                                Margin="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                AutomationProperties.AccessibilityView="Raw"
                                Foreground="{TemplateBinding Foreground}"
                                HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                HorizontalScrollMode="{TemplateBinding ScrollViewer.HorizontalScrollMode}"
                                IsTabStop="False"
                                VerticalScrollBarVisibility="Hidden"
                                VerticalScrollMode="Disabled"
                                ZoomMode="Disabled" />
                            <TextBlock
                                x:Name="PlaceholderTextContentPresenter"
                                Grid.Row="1"
                                Margin="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                Foreground="{Binding PlaceholderForeground, RelativeSource={RelativeSource TemplatedParent}, TargetNullValue={ThemeResource TextControlPlaceholderForeground}}"
                                IsHitTestVisible="False"
                                Text="{TemplateBinding PlaceholderText}"
                                TextAlignment="{TemplateBinding TextAlignment}"
                                TextWrapping="{TemplateBinding TextWrapping}" />
                            <ContentPresenter
                                x:Name="DescriptionPresenter"
                                Grid.Row="2"
                                x:Load="False"
                                AutomationProperties.AccessibilityView="Raw"
                                Content="{TemplateBinding Description}"
                                Foreground="{ThemeResource SystemControlDescriptionTextForegroundBrush}" />
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="Disabled">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="HeaderContentPresenter" Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlHeaderForegroundDisabled}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlBackgroundDisabled}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="BorderBrush">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlBorderBrushDisabled}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentElement" Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlForegroundDisabled}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="PlaceholderTextContentPresenter" Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{Binding PlaceholderForeground, RelativeSource={RelativeSource TemplatedParent}, TargetNullValue={ThemeResource TextControlPlaceholderForegroundDisabled}}" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="BorderBrush">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource ControlStrokeColorDefaultBrush}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlBackgroundPointerOver}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="PlaceholderTextContentPresenter" Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{Binding PlaceholderForeground, RelativeSource={RelativeSource TemplatedParent}, TargetNullValue={ThemeResource TextControlPlaceholderForegroundPointerOver}}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentElement" Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlForegroundPointerOver}" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Focused">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="PlaceholderTextContentPresenter" Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{Binding PlaceholderForeground, RelativeSource={RelativeSource TemplatedParent}, TargetNullValue={ThemeResource TextControlPlaceholderForegroundFocused}}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlBackgroundFocused}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="BorderBrush">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlBorderBrushFocused}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="BorderThickness">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlBorderThemeThicknessFocused}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentElement" Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource TextControlForegroundFocused}" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="HistoryArrowButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="MinWidth" Value="0" />
            <Setter Property="MinHeight" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid Background="{TemplateBinding Background}">
                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Padding="12,12,12,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer
            Grid.Row="0"
            Padding="0,0,14,0"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="24" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel x:Name="LeftColumnPanel" Grid.Row="0" Grid.Column="0">
                    <TextBlock x:Name="ArchiveFolderText" TextWrapping="Wrap" />
                    <Grid Margin="0,4,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0">
                            <TextBox
                                x:Name="ArchivePathBox"
                                Style="{StaticResource PathTextBoxStyle}"
                                MinWidth="140"
                                HorizontalAlignment="Stretch"
                                LostFocus="OnArchivePathChanged"
                                TextChanged="OnArchivePathTextChanged"
                                PreviewKeyDown="OnPathTextBoxPreviewKeyDown">
                                <FlyoutBase.AttachedFlyout>
                                    <Flyout
                                        x:Name="HistoryFlyout"
                                        Placement="Bottom"
                                        Opening="OnHistoryFlyoutOpening"
                                        Opened="OnHistoryFlyoutOpened"
                                        Closed="OnHistoryFlyoutClosed">
                                        <Flyout.FlyoutPresenterStyle>
                                            <Style TargetType="FlyoutPresenter">
                                                <Setter Property="Padding" Value="0" />
                                                <Setter Property="MinWidth" Value="0" />
                                                <Setter Property="MaxWidth" Value="800" />
                                                <Setter Property="CornerRadius" Value="{ThemeResource OverlayCornerRadius}" />
                                                <Setter Property="Background" Value="{ThemeResource FlyoutPresenterBackground}" />
                                            </Style>
                                        </Flyout.FlyoutPresenterStyle>
                                        <ListView
                                            x:Name="HistoryList"
                                            IsItemClickEnabled="True"
                                            SelectionMode="None"
                                            MaxHeight="240"
                                            CornerRadius="{ThemeResource OverlayCornerRadius}"
                                            ItemClick="OnHistoryItemClicked">
                                            <ListView.ItemContainerStyle>
                                                <Style TargetType="ListViewItem">
                                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                    <Setter Property="MinHeight" Value="0" />
                                                    <Setter Property="Padding" Value="10,6,6,6" />
                                                </Style>
                                            </ListView.ItemContainerStyle>
                                            <ListView.ItemTemplate>
                                                <DataTemplate>
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*" />
                                                            <ColumnDefinition Width="Auto" />
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock
                                                            Grid.Column="0"
                                                            Text="{Binding}"
                                                            TextWrapping="NoWrap"
                                                            TextTrimming="CharacterEllipsis"
                                                            VerticalAlignment="Center"
                                                            Margin="0,0,4,0" />
                                                        <Button
                                                            Grid.Column="1"
                                                            Content="&#x2715;"
                                                            Click="OnDeleteHistoryPathClicked"
                                                            ClickMode="Press"
                                                            Background="Transparent"
                                                            BorderThickness="0"
                                                            Padding="4"
                                                            MinWidth="0"
                                                            MinHeight="0"
                                                            VerticalAlignment="Center" />
                                                    </Grid>
                                                </DataTemplate>
                                            </ListView.ItemTemplate>
                                        </ListView>
                                    </Flyout>
                                </FlyoutBase.AttachedFlyout>
                            </TextBox>
                            <Button
                                x:Name="HistoryButton"
                                Style="{StaticResource HistoryArrowButtonStyle}"
                                HorizontalAlignment="Right"
                                Width="32"
                                VerticalAlignment="Stretch"
                                IsTabStop="False"
                                Click="OnHistoryButtonClicked">
                                <FontIcon
                                    FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                    FontSize="10"
                                    Glyph="&#xE70D;"
                                    Foreground="{ThemeResource TextControlForeground}" />
                            </Button>
                        </Grid>
                        <Button
                            x:Name="BrowseButton"
                            Grid.Column="1"
                            Margin="6,0,0,0"
                            Content="..."
                            Click="OnBrowseClicked" />
                    </Grid>

                    <!-- 格式行：标签 + 下拉同行；窗口压窄时下拉换到下一行顶格（与左列所有行同步） -->
                    <Grid x:Name="FormatRow" Margin="0,10,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            x:Name="FormatText"
                            Grid.Row="0"
                            Grid.Column="0"
                            VerticalAlignment="Center" />
                        <ComboBox
                            x:Name="FormatCombo"
                            Grid.Row="0"
                            Grid.Column="1"
                            Margin="6,0,0,0"
                            MinWidth="140"
                            HorizontalAlignment="Stretch"
                            ItemTemplate="{StaticResource WrapItemTemplate}"
                            SelectionChanged="OnFormatChanged" />
                    </Grid>

                    <Grid x:Name="LevelRow" Margin="0,8,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            x:Name="LevelText"
                            Grid.Row="0"
                            Grid.Column="0"
                            VerticalAlignment="Center" />
                        <ComboBox
                            x:Name="LevelCombo"
                            Grid.Row="0"
                            Grid.Column="1"
                            Margin="6,0,0,0"
                            MinWidth="140"
                            HorizontalAlignment="Stretch"
                            ItemTemplate="{StaticResource WrapItemTemplate}"
                            SelectionChanged="OnLevelChanged" />
                    </Grid>

                    <Grid x:Name="MethodRow" Margin="0,8,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            x:Name="MethodText"
                            Grid.Row="0"
                            Grid.Column="0"
                            VerticalAlignment="Center" />
                        <ComboBox
                            x:Name="MethodCombo"
                            Grid.Row="0"
                            Grid.Column="1"
                            Margin="6,0,0,0"
                            MinWidth="140"
                            HorizontalAlignment="Stretch"
                            ItemTemplate="{StaticResource WrapItemTemplate}"
                            SelectionChanged="OnMethodChanged" />
                    </Grid>

                    <Grid x:Name="DictionaryRow" Margin="0,8,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            x:Name="DictionaryText"
                            Grid.Row="0"
                            Grid.Column="0"
                            VerticalAlignment="Center" />
                        <ComboBox
                            x:Name="DictionaryCombo"
                            Grid.Row="0"
                            Grid.Column="1"
                            Margin="6,0,0,0"
                            MinWidth="140"
                            HorizontalAlignment="Stretch"
                            ItemTemplate="{StaticResource WrapItemTemplate}"
                            SelectionChanged="OnDictionaryChanged" />
                    </Grid>

                    <Grid x:Name="OrderRow" Margin="0,8,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            x:Name="OrderText"
                            Grid.Row="0"
                            Grid.Column="0"
                            VerticalAlignment="Center" />
                        <ComboBox
                            x:Name="OrderCombo"
                            Grid.Row="0"
                            Grid.Column="1"
                            Margin="6,0,0,0"
                            MinWidth="140"
                            HorizontalAlignment="Stretch"
                            ItemTemplate="{StaticResource WrapItemTemplate}"
                            SelectionChanged="OnOrderChanged" />
                    </Grid>

                    <Grid x:Name="SolidRow" Margin="0,8,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            x:Name="SolidText"
                            Grid.Row="0"
                            Grid.Column="0"
                            VerticalAlignment="Center" />
                        <ComboBox
                            x:Name="SolidCombo"
                            Grid.Row="0"
                            Grid.Column="1"
                            Margin="6,0,0,0"
                            MinWidth="140"
                            HorizontalAlignment="Stretch"
                            ItemTemplate="{StaticResource WrapItemTemplate}"
                            SelectionChanged="OnSolidChanged" />
                    </Grid>

                    <Grid x:Name="ThreadsRow" Margin="0,8,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            x:Name="ThreadsText"
                            Grid.Row="0"
                            Grid.Column="0"
                            VerticalAlignment="Center" />
                        <ComboBox
                            x:Name="ThreadsCombo"
                            Grid.Row="0"
                            Grid.Column="1"
                            Margin="6,0,0,0"
                            MinWidth="140"
                            HorizontalAlignment="Stretch"
                            ItemTemplate="{StaticResource WrapItemTemplate}"
                            SelectionChanged="OnThreadsChanged" />
                    </Grid>
                    <TextBlock x:Name="HardwareThreadsText" Opacity="0.8" TextWrapping="Wrap" />

                    <Grid x:Name="MemoryRow" Margin="0,8,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            x:Name="MemoryText"
                            Grid.Row="0"
                            Grid.Column="0"
                            VerticalAlignment="Center" />
                        <ComboBox
                            x:Name="MemoryCombo"
                            Grid.Row="0"
                            Grid.Column="1"
                            Margin="6,0,0,0"
                            MinWidth="140"
                            HorizontalAlignment="Stretch"
                            ItemTemplate="{StaticResource WrapItemTemplate}"
                            SelectionChanged="OnMemoryChanged" />
                        <TextBlock
                            x:Name="MemoryValueText"
                            Grid.Row="1"
                            Grid.Column="0"
                            Grid.ColumnSpan="2"
                            Margin="0,4,0,0"
                            TextWrapping="NoWrap"
                            TextTrimming="CharacterEllipsis" />
                    </Grid>

                    <Grid x:Name="VolumeRow" Margin="0,8,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            x:Name="VolumeText"
                            Grid.Row="0"
                            Grid.Column="0"
                            VerticalAlignment="Center" />
                        <ComboBox
                            x:Name="VolumeCombo"
                            Grid.Row="0"
                            Grid.Column="1"
                            Margin="6,0,0,0"
                            MinWidth="140"
                            HorizontalAlignment="Stretch"
                            IsEditable="True"
                            ItemTemplate="{StaticResource WrapItemTemplate}"
                            LostFocus="OnVolumeChanged" />
                    </Grid>

                    <Grid x:Name="ParametersRow" Margin="0,8,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            x:Name="ParametersText"
                            Grid.Row="0"
                            Grid.Column="0"
                            VerticalAlignment="Center" />
                        <TextBox
                            x:Name="ParametersBox"
                            Grid.Row="0"
                            Grid.Column="1"
                            Margin="6,0,0,0"
                            AcceptsReturn="True"
                            TextWrapping="Wrap"
                            MinHeight="36"
                            TextChanged="OnParametersChanged" />
                    </Grid>

                    <Grid Margin="0,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Button
                            x:Name="OptionsButton"
                            Content="Options"
                            Click="OnOptionsClicked" />
                        <TextBlock
                            x:Name="OptionsSummaryText"
                            Grid.Column="1"
                            Margin="8,0,0,0"
                            VerticalAlignment="Center"
                            TextWrapping="Wrap" />
                    </Grid>
                </StackPanel>

                <StackPanel x:Name="RightColumnPanel" Grid.Row="0" Grid.Column="2">
                    <Grid x:Name="UpdateModeRow">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            x:Name="UpdateModeText"
                            Grid.Row="0"
                            Grid.Column="0"
                            VerticalAlignment="Center" />
                        <ComboBox
                            x:Name="UpdateModeCombo"
                            Grid.Row="0"
                            Grid.Column="1"
                            Margin="6,0,0,0"
                            MinWidth="140"
                            HorizontalAlignment="Stretch"
                            ItemTemplate="{StaticResource WrapItemTemplate}"
                            SelectionChanged="OnUpdateModeChanged" />
                    </Grid>

                    <Grid x:Name="PathModeRow" Margin="0,8,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            x:Name="PathModeText"
                            Grid.Row="0"
                            Grid.Column="0"
                            VerticalAlignment="Center" />
                        <ComboBox
                            x:Name="PathModeCombo"
                            Grid.Row="0"
                            Grid.Column="1"
                            Margin="6,0,0,0"
                            MinWidth="140"
                            HorizontalAlignment="Stretch"
                            ItemTemplate="{StaticResource WrapItemTemplate}"
                            SelectionChanged="OnPathModeChanged" />
                    </Grid>

                    <Border
                        x:Name="OptionsBorder"
                        Margin="0,12,0,0"
                        BorderBrush="#80808080"
                        BorderThickness="1"
                        CornerRadius="2"
                        Padding="8">
                        <StackPanel>
                            <TextBlock x:Name="OptionsGroupText" TextWrapping="Wrap" />
                            <CheckBox
                                x:Name="SfxCheck"
                                Margin="0,8,0,0"
                                ContentTemplate="{StaticResource WrapItemTemplate}"
                                Click="OnSfxClicked" />
                            <CheckBox
                                x:Name="SharedCheck"
                                Margin="0,6,0,0"
                                ContentTemplate="{StaticResource WrapItemTemplate}"
                                Click="OnSharedClicked" />
                            <CheckBox
                                x:Name="DeleteCheck"
                                Margin="0,6,0,0"
                                ContentTemplate="{StaticResource WrapItemTemplate}"
                                Click="OnDeleteClicked" />
                        </StackPanel>
                    </Border>

                    <Border
                        x:Name="EncryptionBorder"
                        Margin="0,12,0,0"
                        BorderBrush="#80808080"
                        BorderThickness="1"
                        CornerRadius="2"
                        Padding="8">
                        <StackPanel>
                            <TextBlock x:Name="EncryptionGroupText" TextWrapping="Wrap" />
                            <TextBlock x:Name="PasswordText" Margin="0,8,0,0" />
                            <PasswordBox
                                x:Name="PasswordBox"
                                Margin="0,6,0,0"
                                Background="{ThemeResource TextControlBackground}"
                                Foreground="{ThemeResource TextControlForeground}"
                                BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                BorderThickness="{ThemeResource TextControlBorderThemeThickness}" />
                            <TextBlock x:Name="ReenterPasswordText" Margin="0,8,0,0" />
                            <PasswordBox
                                x:Name="ReenterPasswordBox"
                                Margin="0,6,0,0"
                                Background="{ThemeResource TextControlBackground}"
                                Foreground="{ThemeResource TextControlForeground}"
                                BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                BorderThickness="{ThemeResource TextControlBorderThemeThickness}" />
                            <CheckBox
                                x:Name="ShowPasswordCheck"
                                Margin="0,6,0,0"
                                ContentTemplate="{StaticResource WrapItemTemplate}"
                                Click="OnShowPasswordClicked" />
                            <!-- 加密算法行：标签与勾选框文本左对齐（Margin 28）；默认同行，窗口压得很窄时才换行 -->
                            <Grid x:Name="EncryptionMethodRow" Margin="0,8,0,0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock
                                    x:Name="EncryptionMethodText"
                                    Grid.Row="0"
                                    Grid.Column="0"
                                    Margin="28,0,0,0"
                                    VerticalAlignment="Center" />
                                <ComboBox
                                    x:Name="EncryptionMethodCombo"
                                    Grid.Row="0"
                                    Grid.Column="1"
                                    Margin="6,0,0,0"
                                    MinWidth="140"
                                    HorizontalAlignment="Stretch"
                                    ItemTemplate="{StaticResource WrapItemTemplate}"
                                    SelectionChanged="OnEncryptionMethodChanged" />
                            </Grid>
                            <CheckBox
                                x:Name="EncryptHeadersCheck"
                                Margin="0,6,0,0"
                                ContentTemplate="{StaticResource WrapItemTemplate}"
                                Click="OnEncryptHeadersClicked" />
                        </StackPanel>
                    </Border>
                </StackPanel>

                <TextBlock
                    x:Name="ErrorTextBlock"
                    Grid.Row="1"
                    Grid.Column="0"
                    Grid.ColumnSpan="3"
                    Margin="0,8,0,0"
                    Foreground="#FFE06C75"
                    TextWrapping="Wrap"
                    Visibility="Collapsed" />
            </Grid>
        </ScrollViewer>

        <StackPanel
            Grid.Row="1"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            Margin="0,12,0,0">
            <Button
                x:Name="OkButton"
                MinWidth="88"
                Margin="0,0,8,0"
                Content="OK"
                Click="OnOkClicked" />
            <Button
                x:Name="CancelButton"
                MinWidth="88"
                Content="Cancel"
                Click="OnCancelClicked" />
        </StackPanel>
    </Grid>
</Page>
